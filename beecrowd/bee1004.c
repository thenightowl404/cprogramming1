#include <stdio.h>

int main() {
    int A, B, PROD;
    
    // Read two integer values
    scanf("%d %d", &A, &B);
    
    // Calculate the product
    PROD = A * B;
    
    // Print the result with the required format
    printf("PROD = %d\n", PROD);
    
    return 0;
}